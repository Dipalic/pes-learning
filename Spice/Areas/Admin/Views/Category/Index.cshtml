
@model IEnumerable<Category>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<br />
<br />
<div class="border backgroundWhite">
    <div class="row">
        <div class="col-6">
            <h2 class="text-info"> Category List</h2>
        </div>
        <div class="col-6 text-right">
            <partial name="_CreateButtonPartial" />
        </div>
    </div>
    <br />
    <div>
        @if (Model.Count() > 0)
        {
            <table class="table table-striped border">
                <tr class="table-secondary">
                    <th>
                        @Html.DisplayNameFor(m => m.Name)
                    </th>
                    <th></th>
                    <th></th>
                </tr>
                @foreach (var item in Model)
                {
            <tr>
                <td>
                    @*<a href="/Admin/SubCategory/Index/" +@Html.Display(item.Id)>@Html.DisplayFor(m => item.Name)</a>*@
                    <a href="@("/Admin/SubCategory/Index/"+item.Id)">@Html.DisplayFor(m => item.Name)</a>
                    

                    @*<div class="nav-item dropdown text-white-50">
            <button class="nav-link dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                @Html.DisplayFor(m => item.Name)
            </button>

            <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                <a class="dropdown-item" href="Admin/Category/Index">Category</a>
                <a class="dropdown-item" href="#">Sub Category</a>
                <a class="dropdown-item" href="#">Menu</a>
                <a class="dropdown-item" href="#">Coupons</a>
                <a class="dropdown-item" href="#">Users</a>

            </div>
        </div>*@
                </td>
                
                
                   <td>
                    <partial name="_TableButtonPartial" model="@item.Id" />
                </td>
            </tr>
                }
            </table>
        }
        else
        {
            <p>No category exists...</p>
        }
    </div>
</div>